import styled from 'styled-components';

const StyledInfo = styled.div`
  input {
    border: none;
    outline: none;
    padding: 0.1rem 0;
  }

  input:focus {
    border: none;
  }
`;

export default StyledInfo;
