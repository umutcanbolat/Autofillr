import styled from 'styled-components';

const StyledInfo = styled.div`
  input {
    border: none;
    outline: none;
    width: 100%;
  }

  input:focus {
    border: none;
  }
`;

export default StyledInfo;
